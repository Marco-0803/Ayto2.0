/* === 1. Allgemeines Layout & Body === */
body {
  margin: 0;
  font-family: "Segoe UI", Roboto, sans-serif;
  background: #0e0f1a;
  color: #f5f5f5;
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

/* === 2. Header & Logo (Vollbild-Anpassung) === */
header {
  text-align: center;
  background: linear-gradient(90deg, #ff4fa8, #4a82ff, #ff4fa8);
  background-size: 200% 200%;
  animation: gradientShift 12s ease infinite;
  border-bottom: 1px solid rgba(255,255,255,0.1);
  padding: 0; /* Padding auf 0, damit Logo Ränder berührt */
  overflow: hidden;
}

@keyframes gradientShift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

header img.logo {
  width: 100%;      /* Füllt die komplette Breite aus */
  max-width: 100%;
  height: auto;     /* Verhindert Verzerrung */
  display: block;
  margin: 0;
}

header h1 {
  font-size: 1.2rem;
  margin: 10px 0 5px 0;
  color: #fff;
}

header .small {
  font-size: 12px;
  color: rgba(255,255,255,0.8);
  padding-bottom: 10px;
}

/* === 3. Hauptcontainer & Seiten === */
main {
  flex: 1;
  padding-bottom: 80px; /* Platz für Nav unten */
}

.page {
  display: none;
  padding: 12px;
}

.page.active {
  display: block;
}

.card {
  background: rgba(30, 33, 60, 0.9);
  border-radius: 10px;
  padding: 14px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.4);
  margin-bottom: 14px;
}

.stack > *:not(:last-child) {
  margin-bottom: 8px;
}

/* === 4. Buttons === */
button {
  border: none;
  border-radius: 6px;
  padding: 8px 12px;
  cursor: pointer;
  font-size: 14px;
  transition: background 0.3s;
}

button.primary { background: #346fff; color: #fff; width: 100%; }
button.primary:hover { background: #4a82ff; }

button.ghost { background: rgba(255,255,255,0.1); color: #fff; }
button.ghost:hover { background: rgba(255,255,255,0.2); }

button.danger { background: #c42c2c; color: #fff; }
button.small { font-size: 12px; padding: 5px 8px; }

/* === 5. Navigation unten === */
.bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  display: flex;
  background: rgba(20, 22, 35, 0.98);
  border-top: 1px solid rgba(255,255,255,0.1);
  padding: 6px 0;
  backdrop-filter: blur(10px);
  z-index: 999;
}

.bottom-nav button {
  flex: 1;
  background: none;
  color: #777;
  font-size: 11px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 3px;
}

.bottom-nav button.active {
  color: #4a82ff;
  font-weight: bold;
}

.bottom-nav .icon { font-size: 18px; }

/* === 6. Overlay (Ladebildschirm) === */
#overlay {
  position: fixed;
  inset: 0;
  background: rgba(14, 15, 26, 0.95); /* Passend zu deinem dunklen Body */
  display: none; 
  align-items: center;
  justify-content: center;
  z-index: 10000;
  backdrop-filter: blur(10px);
}

#overlay.show {
  display: flex;
}

.overlay-box {
  background: #1e213c; /* Deine Kartenfarbe */
  padding: 30px;
  border-radius: 20px;
  text-align: center;
  width: 80%;
  max-width: 350px;
  border: 1px solid rgba(255,255,255,0.1);
  box-shadow: 0 10px 30px rgba(0,0,0,0.5);
}

.overlay-logo {
  width: 120px;
  height: auto;
  margin-bottom: 20px;
  animation: pulse 2s infinite ease-in-out;
}

.status-text {
  margin: 15px 0;
  font-size: 1.1rem;
  color: #fff;
}

.progress {
  width: 100%;
  height: 12px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 6px;
  overflow: hidden;
}

.progress .bar {
  width: 0%; /* Startet bei 0 */
  height: 100%;
  background: linear-gradient(90deg, #ff4fa8, #4a82ff); /* Dein Pink-Blau Verlauf */
  transition: width 0.3s ease; /* Macht die Bewegung flüssig */
}

@keyframes pulse {
  0% { transform: scale(1); opacity: 0.8; }
  50% { transform: scale(1.05); opacity: 1; }
  100% { transform: scale(1); opacity: 0.8; }
}


/* === 7. Matrix Tabelle (Ergebnisse) === */
.ayto-table-container {
  overflow-x: auto;
  border-radius: 8px;
  margin-top: 10px;
}

.ayto-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 12px;
}

.ayto-table th, .ayto-table td {
  padding: 10px 5px;
  text-align: center;
  border: 1px solid #1a1d2e;
  min-width: 70px;
}

.ayto-table th {
  background: #23283f;
  position: sticky;
  top: 0;
}

.a-name {
  position: sticky;
  left: 0;
  background: #23283f !important;
  font-weight: bold;
  z-index: 2;
  text-align: left;
}

.no-match {
  background: #2d334a !important;
  color: #666 !important;
  font-style: italic;
  font-size: 10px;
}

/* === 8. Inputs & Listen === */
input[type="text"], select {
  background: rgba(255,255,255,0.05);
  border: 1px solid #333;
  border-radius: 5px;
  color: #fff;
  padding: 8px;
  width: 100%;
  box-sizing: border-box;
}

.list .row {
  display: flex;
  gap: 8px;
  margin-bottom: 8px;
}
